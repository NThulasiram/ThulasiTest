@model FGMC.RMS.Web.Models.ADReportsModel
<link href="~/Content/styles.css" rel="stylesheet" />
<script src="~/Scripts/rms/adreport.js"></script>
@*Report Specific Scripts*@
<script src="https://cdn.datatables.net/buttons/1.2.2/js/dataTables.buttons.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jszip/2.5.0/jszip.min.js"></script>
<script src="https://cdn.datatables.net/buttons/1.2.2/js/buttons.html5.min.js"></script>
<link href="https://cdn.datatables.net/buttons/1.2.2/css/buttons.dataTables.min.css" rel="stylesheet" />
<div class="row">
    <div class="col-sm-3 col-lg-2">
        <div class="form-group">
            <label for="username">Enter User Name</label>
            @Html.DropDownListFor(m => m.SelectedADUser, new SelectList(Model.ADUsers, "ADUserID", "ADUserName"), "Select User", new { id = "ddlUser", @class = "form-control" })
        </div>
    </div>
    <div class="col-sm-3 col-lg-2">
        <label for="userid" class="search_lbl"></label>
        <div class="form-group">
            <button type="button" class="btn btn-success" id="btnSearch">Generate Report</button>
        </div>
    </div>
</div>
<div class="content">
    <h4 class="content-title">@Model.Title</h4>
    <table id="RequestSummary" class="table table-striped table-bordered" cellspacing="0">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>User Name</th>
                <th>Branch</th>
                <th>Requested Date</th>
                <th>Manage Engine Ticket ID</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.ReportData)
            {
                <tr>
                    <td>@item.FirstName</td>
                    <td>@item.LastName</td>
                    <td>@item.ADUserName</td>
                    <td>@item.GroupName</td>
                    <td>@item.RequestDate.ToShortDateString()</td>
                    <td>@item.MERequestId</td>
                </tr>
            }
        </tbody>
    </table>
    <span style="color:red">@Model.ErrorMessage</span>
</div>
